---
platforms:
  ubuntu1404:
    build_targets:
    - "..."
    test_targets:
    - "--"
    - "..."
    # This test is currently failing on ubuntu on CI (but 
    # passes when manually run on ubuntu.
    # TODO(cparsons): Fix and enable.
    - "-//tests:unittest_e2e_test"
  ubuntu1604:
    build_targets:
    - "..."
    test_targets:
    - "--"
    - "..."
    # This test is currently failing on ubuntu on CI (but 
    # passes when manually run on ubuntu.
    # TODO(cparsons): Fix and enable.
    - "-//tests:unittest_e2e_test"
  macos:
    build_targets:
    - "..."
    test_targets:
    - "..."
    test_flags:
    - "--test_env=PATH"
  windows:
    build_targets:
    - "..."
    test_targets:
    - "--"
    - "..."
    # Shell test doesn't run on windows.
    - "-//tests:unittest_e2e_test"
